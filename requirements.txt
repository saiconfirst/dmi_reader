# Зависимости для dmi_reader
# Только для Windows (опционально)
wmi>=1.5.1 ; sys_platform == "win32"
pywin32>=227 ; sys_platform == "win32"

# Дополнительно (для тестов, линтеров, документации)
pytest>=7.0.0
mypy>=1.0.0
flake8>=5.0.0
black>=23.0.0